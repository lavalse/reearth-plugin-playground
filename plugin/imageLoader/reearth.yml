id: image-loader
name: image loader
version: 1.0.0
extensions:
  - id: image-loader
    type: widget
    name: image loader
    schema:
      groups:
        - id: default
          fields:
            - id: image
              type: url
              title: image url
